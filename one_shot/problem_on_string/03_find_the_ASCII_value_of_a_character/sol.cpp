#include <bits/stdc++.h>
using namespace std;

void get_ASCII_value(char c) {
  cout << (int)c << endl;
} // (int) typecast is used to convert the character to its ASCII value.

int main() {
  char c;
  cin >> c;
  get_ASCII_value(c);
  return 0;
}
